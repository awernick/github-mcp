# Fine-grained PAT with Issues r/w, Pull requests r/w, Contents r,
# Checks r (CI summary; optional - get_pull_request degrades gracefully
# without it), Metadata r (auto) on the repos to manage.
GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_...
