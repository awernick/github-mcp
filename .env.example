# Fine-grained PAT with Issues r/w, Pull requests r/w, Contents r
# (releases need no extra permission), Metadata r (auto; covers the CI
# check summary) on the repos to manage. Actions r only if workflow-run
# tools are added later.
GITHUB_PERSONAL_ACCESS_TOKEN=github_pat_...
